{%  extends  'CarouselBundle::layout.html.twig' %}

{% block body -%}


    <div class="container">
        <br>
        <div id="myCarousel" class="carousel slide" data-ride="carousel">
            <!-- Indicators -->
            <ol class="carousel-indicators">
    {% if objects is defined %}
        {% for object in objects  %}
            {% if loop.index == 1 %}
                <li data-target="#myCarousel" data-slide-to={{ loop.index - 1 }} class="active"></li>
            {% else %}
                <li data-target="#myCarousel" data-slide-to={{ loop.index - 1 }}></li>
            {% endif %}
        {% endfor %}
    {% endif %}

            </ol>

            <!-- Wrapper for slides -->
            <div class="carousel-inner" role="listbox">
                {% if objects is defined %}
                    {% for object in objects  %}
                        {% if loop.index == 1 %}
                        <div class="item active">
                        <img src="{{ path ~ object.imageName }}" alt="{{ loop.index }}" width="460" height="345">
                            <span class="Text">{{ object.text }}</span>
                        </div>
                        {% else %}
                            <div class="item">
                                <img src=" {{ path ~ object.imageName }}" alt="{{  loop.index }}" width="460" height="345">
                                <span class="Text">{{ object.text }}</span>
                            </div>
                        {% endif %}
                    {% endfor %}

                <!-- Left and right controls -->
                <a class="left carousel-control" href="#myCarousel" role="button" data-slide="prev">
                    <span class="glyphicon glyphicon-chevron-left" aria-hidden="true"></span>
                    <span class="sr-only">Previous</span>
                </a>
                <a class="right carousel-control" href="#myCarousel" role="button" data-slide="next">
                    <span class="glyphicon glyphicon-chevron-right" aria-hidden="true"></span>
                    <span class="sr-only">Next</span>
                </a>
            </div>
                {% else %}
                    <div class="jumbotron">
                        <div class="container">
                            <h1>Start building, now!</h1>
                            <p>SymfonyZero is a free fully functional kickstarter edition,</p>
                            <p>use it as a starting point for your Symfony web projects.</p>
                            <p><a class="btn btn-primary btn-lg" href="https://github.com/Emergya/SymfonyZero" role="button">Get it on Github</a></p>
                        </div>
                    </div>

                {% endif %}
            </div>


        <div class="containerFoot">
            <div class="row">

                <div class="col-xs-12 col-md-4 main-column">
                    <p class="icon-header"><span class="glyphicon glyphicon-cog" aria-hidden="true"></span></p>

                    <p>In this documentation you can learn about how to install, configure, available bundles and sections and how can you help to improve it.</p>
                </div>

                <div class="col-xs-12 col-md-4 main-column">
                    <p class="icon-header"><span class="glyphicon glyphicon-ok-circle" aria-hidden="true"></span></p>

                    <p>SymfonyZero is an alive project and we'll be adding new features and improvements, so stay tuned for new updates.</p>
                    <p>If you detect an error, you know how to improve it or you have an idea about this edition, please, fix it, do it or write us!</p>
                </div>

                <div class="col-xs-12 col-md-4 main-column">
                    <p class="icon-header"><span class="glyphicon glyphicon-search" aria-hidden="true"></span></p>

                    <p>For further information, please take a look to <a href="{{path('about')}}">About</a></p>
                </div>
            </div>
        </div>
    </div>
{%endblock%}