<!--div id="sidebar-wrapper">
    <h2>Symfony<span class="title-zero">Zero</span></h2>
    <div class="sub-title">based on  {{ constant('Symfony\\Component\\HttpKernel\\Kernel::VERSION') }}</div>
    <a href="{{path('homepage')}}"><img class="sidebar-brand" src="{{asset("bundles/app/images/emergya.png")}}" alt="Emergya"></a><p/>
</div-->

<nav class="navbar navbar-default navbar-fixed-top">
      <div class="container">

        <div class="navbar-header">
          <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="#">Project name</a>
        </div>

        <div id="navbar" class="navbar-collapse collapse">
          {%set isAdmin =  (is_granted('ROLE_ADMIN'))?1:0%}
          {% set menu = knp_menu_get('AppBundle:Builder:mainMenu',[],{'admin':isAdmin}) %}
          {{ knp_menu_render(menu, {'currentClass': 'active'}) }}

          <ul class="nav navbar-nav navbar-right">
            <li><a href="../navbar/">Sign Up</a></li>
            <li><a href="../navbar/">Sign In</a></li>
          </ul-->
        </div>
      </div>
    </nav>
